# BEGIN DOTFILES EXCLUDE TEMPLATE
# Copy this template to new environment exclude files.

# Dotfiles exclude template from https://github.com/sparr/dotfiles/blob/main/.git.common/info/exclude

# Using ~ as a git repo requires excluding all files by default
*

# Then we can un-exclude specific files

# directories must be explicitly un-excluded before files within them
# Change ".git.common" to ".git.NEWENVNAME" for new environments
!/.git.common/
!/.git.common/info/
!/.git.common/info/exclude # this file

# END DOTFILES EXCLUDE TEMPLATE

!/README.md

# shell setup
!/.aliases
!/.bash_logout
!/.bash_profile
!/.bashrc
!/.bashrc.common
!/.env
!/.funcs
!/.path
!/.shellrc
!/.shellrc.common
!/.zshrc
!/.zshrc.common

# ssh
!/.ssh/
!/.ssh/config

# config directory
!/.config/

# github cli
!/.config/gh/
!/.config/gh/config.yml

# git
!/.config/git/ # ineffective
!/.config/git/config
!/.config/git/ignore

# vim
!/.config/vim/
!/.config/vim/vimrc

# npm
!/.config/npm/
!/.config/npm/config

# maven
!/.config/maven/
!/.config/maven/settings.xml
