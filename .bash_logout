#!/bin/bash
# ~/.bash_logout
# bash runs this script when a login shell logs out

# This script provides functions to run environment-specific variants of other scripts
. "${HOME}"/.funcs

other_envs_source .bash_logout